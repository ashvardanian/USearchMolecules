# Small in-repo binaries tracked with Git LFS. Dataset shards are not in Git —
# they live on the S3, Nebius, and Hugging Face mirrors; see the README.
cdk-2.2.jar filter=lfs diff=lfs merge=lfs -text
*.jsonl filter=lfs diff=lfs merge=lfs -text
